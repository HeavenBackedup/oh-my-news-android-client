<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.wangyan.oh_my_news_android_client">

    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.MOUNT_UNMOUNT_FILESYSTEMS" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity" />
        <activity
            android:name=".PersonalPageActivity"
            android:screenOrientation="portrait" />
        <activity android:name=".activities.BaseActivity" />
        <activity android:name=".activities.MyCollectionActivity" />
        <activity android:name=".activities.MyArticleActivity" />
        <activity android:name=".activities.MyFansListActivity" />
        <activity android:name=".activities.OthersHomepageActivity"></activity>
        <activity android:name=".activities.HomepageActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activities.MainpageActivity">

        </activity>
        <activity android:name=".activities.LoginActivity"></activity>
        <activity android:name=".activities.DetailActivity" />
        <activity android:name=".activities.SearchActivity" />
        <activity android:name=".activities.PrivateMsgActivity" />
        <activity android:name=".activities.ConcernsActivity"></activity>

        <service
            android:name=".services.MainpageService"
            android:enabled="true"
            android:exported="true" />

        <activity android:name=".activities.RegistActivity"></activity>

        <service
            android:name=".services.LoginService"
            android:enabled="true"
            android:exported="true" />
        <service
            android:name=".services.RegistService"
            android:enabled="true"
            android:exported="true" />

        <activity android:name=".activities.StartActivity">

        </activity>
    </application>

</manifest>